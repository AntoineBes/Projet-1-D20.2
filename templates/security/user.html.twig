{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}

{% for user in users %}

<p>{{ user.email }}</p>
<p>{{ user.firstname }}</p>
<p>{{ user.lastname }}</p>
<a href="{{ path( 'removeuser_id', {'id': user.id}) }} ">Delete</a>


{% endfor %}
{% endblock %}
